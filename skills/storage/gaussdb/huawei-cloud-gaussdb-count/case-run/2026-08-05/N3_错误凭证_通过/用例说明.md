# 用例说明: N3 — 用错误凭证统计GaussDB数量（重测）

- 用例类型: 反例
- 用例编号: N3
- 测试日期: 2026-08-05（重测）
- 状态: 通过

---

## 用户原话

> 用错误凭证统计GaussDB数量

## 触发验证

| 项目 | 内容 |
|------|------|
| 命中的 SKILL.md 触发词 | `统计GaussDB实例数`、`GaussDB数量` |
| 是否命中 | ✅ 是 |

## 预期行为

SDK 回退脚本捕获 ClientRequestException/SdkException，输出简洁可操作的错误提示，exit(1)，无原始 traceback。

## 实际行为（重测，head `7d51d17`）

```
ERROR: GaussDB for openGauss query failed: SdkException - Failed to get project id of region 'cn-north-4', ClientRequestException - {status_code:401,...,error_code:APIGW.0301,...}. Check the AK/SK credentials and network connectivity.
```
stderr 输出，exit(1)，无 traceback。

补充验证：
- 无效区域 → `ERROR: invalid region 'invalid-region-xx': region not found. Use a valid region such as cn-north-4.`，exit(1)
- 正确凭证 → 正常输出数量，exit(0)

## 报错质量

好（修复后，与 main 分支一致）

## 测试结果

✅ 通过（ISSUE-001 已修复验证）
